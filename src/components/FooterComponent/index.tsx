import { FC } from 'react';
import { Layout } from 'antd';

const { Footer } = Layout;

export const FooterComponent: FC = () => {
  return <Footer style={{ textAlign: 'center' }}>©2022 — Lectrum LLC</Footer>;
};
